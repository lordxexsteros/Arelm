#ifndef UTILS_H
#define UTILS_H
#include <iostream>
#include <thread>
#include <chrono>

auto cls(){}
auto timer(int sec){}
auto text = [](const std::string &text, int delay_ms){};
auto get_name(std::string user){}

#endif